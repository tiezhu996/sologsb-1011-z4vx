# sologsb-1011 · 实时字幕纠错台

> 端口：宿主 `10011` → 容器 `80`

## 原始需求

我想做一个实时字幕纠错台，用Lit+TypeScript+Carbon Web Components实现。字幕会分段进入待确认区，校对员可以修改标点、专有名词、发言人和数字，给术语添加快捷替换规则，并把确认后的内容送入直播区。遇到延迟或断线时要显示队列积压，允许离线继续校正，恢复后按时间顺序合并；重复片段和过期修改要明确提示。界面要适合长时间使用，支持快捷键、字号调整和键盘导航。
